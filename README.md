# Movie_Recommender
A simple movie recommender using ML with a basic UI

To find the similarities between the movie I cannot import that file on GitHub since it's size is more than 100MB so instead i'm calling that file directly from Google Drive in order to work.
